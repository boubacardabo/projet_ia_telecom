make an environment :
```sh
virtualenv .venv
cd frontend
pip install -r requirements.txt
```


run the application :
```sh
source .venv/Scripts/activate #in bash
cd frontend
streamlit run home.py
```
